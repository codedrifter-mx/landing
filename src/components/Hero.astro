---
---

<section class="hero">
  <p class="prompt">$ whoami</p>
  <h1 class="name">Alfredo Flores Garcia</h1>
  <p class="tagline">Software Engineer &middot; IAM / Keycloak &middot; Dexcom</p>
  <p class="location">Durango, Mexico &mdash; Remote</p>
  <nav class="contact-links" aria-label="Contact links">
    <a href="https://github.com/codedrifter-mx" rel="noopener noreferrer" target="_blank">github<span class="sr-only"> (opens in new tab)</span></a>
    <span class="sep" aria-hidden="true">/</span>
    <a href="https://www.linkedin.com/in/alfredo-flores-mx" rel="noopener noreferrer" target="_blank">linkedin<span class="sr-only"> (opens in new tab)</span></a>
    <span class="sep" aria-hidden="true">/</span>
    <a href="mailto:alfredofloresgarcia156@gmail.com">email</a>
  </nav>
</section>

<style>
  .hero {
    padding: 2rem 0 3rem;
  }
  .prompt {
    color: var(--color-accent);
    font-size: 0.9rem;
    margin: 0 0 0.5rem 0;
  }
  .name {
    font-size: clamp(1.75rem, 5vw, 2.5rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    color: var(--color-ink);
    margin: 0 0 0.5rem 0;
    line-height: 1.2;
  }
  .tagline {
    color: var(--color-ink);
    font-size: 0.95rem;
    margin: 0 0 0.25rem 0;
  }
  .location {
    color: var(--color-ink-dim);
    font-size: 0.85rem;
    margin: 0 0 1.5rem 0;
  }
  .contact-links {
    display: flex;
    gap: 0.5rem;
    align-items: center;
    font-size: 0.9rem;
    flex-wrap: wrap;
  }
  .contact-links a {
    color: var(--color-sky);
  }
  .contact-links a:hover {
    color: var(--color-accent);
  }
  .sep {
    color: var(--color-ink-dim);
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
</style>
